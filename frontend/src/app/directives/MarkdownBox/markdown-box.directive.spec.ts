import { MarkdownBoxDirective } from "./markdown-box.directive";

describe("MarkdownBoxDirective", () => {
  it("should create an instance", () => {
    const directive = new MarkdownBoxDirective();
    expect(directive).toBeTruthy();
  });
});
